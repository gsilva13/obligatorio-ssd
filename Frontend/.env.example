# Variables de entorno para el frontend
# Copia este archivo como .env y configura las variables según tu entorno

# URL base de la API del backend
# En desarrollo local, usar: http://localhost:3001/api
# En producción, cambiar por la URL real del servidor
VITE_API_BASE_URL=http://localhost:3001/api

# Configuración de entorno
# Valores posibles: development, production
VITE_ENV=development

# Puerto del servidor de desarrollo (opcional)
# Si no se especifica, Vite usará un puerto disponible (generalmente 5173, 8080, etc.)
# PORT=8080
